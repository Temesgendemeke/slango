import SubmitSlangForm from "@/components/SubmitSlangForm";
import React from "react";

export const metadata = {
  title: "Submit slang",
};

const page = async () => {
  return <SubmitSlangForm />;
};

export default page;
