import React from "react";
import InvalidToken from "@/components/InvalidToken";

export const meta = {
  title: "invalid password",
};

const Page = () => {
  return <InvalidToken />;
};

export default Page;
